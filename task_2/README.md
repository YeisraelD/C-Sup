## 2
with variable stuff c is almost cpp, found nothing newer on that so , lets move 

wait, here we got some stuff called format specifiers, those start with %. each one must match the variable type unless, we gonna have undefined behavior.

but in c++, we dont have to use format specifiers, cause the compiler knows the the datatype of each variable automatically. << is overloaded for all data types.

doesnt mean that,cpp totally doesnt use specifiers , if u write this sample code in cp file it will definitely run , cause cpp supports c libraries.

## output
```terminal_session
5 is integer
3.140000 is float
hello u is string
``